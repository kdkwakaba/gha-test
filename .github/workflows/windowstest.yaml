name: Windows Test

on:
  push:

jobs:
  job1:
    name: Execute job1
    runs-on: self-hosted
    steps:
      - name: Git clone
        uses: actions/checkout@v4
    
      - name: job1 echo
        run: |
          dir
          echo "job1!"
